# Optional OpenAI-compatible model provider.
# Copy this file to .env and replace the placeholder locally.
# Never commit a real API key.
OPENAI_MODEL=gpt-5.5
OPENAI_API_KEY=replace-with-your-local-key
OPENAI_BASE_URL=https://api.xairouter.com/v1
